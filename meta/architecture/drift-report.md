# Drift Report — Adverax CRM

**Generated by** `forge:reverse drift` on **2026-06-28**

## Scope

Re-actualized the as-built model against current code. The model was built earlier the
same day from this same tree, so **no structural drift** was expected or found — the
delta is the prescriptive AC↔test binding (pass 6b) that the onboarding run had deferred.

## Deltas

### In code, not in model → added
- None structurally. All 18 components, 8 contexts, 20 aggregates still match the code.

### In model, not in code → stale
- None. No tombstones needed.

### Model says A, code does B → conflict
- None.

### Prescriptive overlay updated (pass 6b — test mining)
- **25 requirements** bound to real, verified test functions (33 distinct tests; grounding
  check 33/33 present in `*_test.go`).
- trace.yml: **24 requirements → `covered`**, **4 remain `partial`**.
- requirements.yml: NFR-001/002/004 gained executable acceptance blocks.
- Validator warnings: **121 → 72**. CI gate: **PASSED** (0 errors).

## Remaining gaps (for a real test-writing pass, not reverse)

These are honest coverage holes in the **code's own test suite**, surfaced — not invented:

| Req | Gap | Suggested test location |
|-----|-----|-------------------------|
| FR-008 | No end-to-end RLS visibility test (only cache builder tested) | `internal/platform/soql/` |
| FR-012 | No SOQL limit-rejection test | `internal/platform/soql/` |
| FR-021 | Automation engine has **no Go unit test** (e2e only) | `internal/platform/automation/` |
| NFR-003 | No performance/scope assertion for incremental recompute | `internal/platform/security/` |

## Verdict

Model is **consistent with code** and now **test-anchored**. Reverse-engineered entries
remain tagged `source: reverse-engineered`; curate to `authored` as a human confirms intent.
Forge can evolve forward normally (`/forge:roadmap`, `/forge:kanban`, `/forge:explain`).
